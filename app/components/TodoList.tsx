import React from 'react';

const TodoList = () => {
    return (
        <div>
            <h2>Todo List</h2>
            {/* Add implementation later */}
        </div>
    );
};

export default TodoList;